import './App.css';

function App() {
  
//Create a permanent link with js/background.js
/*  function createListener() {
    chrome.runtime.onConnect.addListener((port) => {
      port.onMessage.addListener(callback);
    });
  }*/


  return (
    <div className="App">
      <header className="App-header">
        Hello World
      </header>
    </div>
  );
}

export default App;
