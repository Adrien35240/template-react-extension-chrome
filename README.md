## Une application simple qui renvoie le titre de la page visité lorsque l'on clique sur l'icone.
### Installation sur chrome en mode developpeur.
```
npm i && npm run build
```
UpLoader le dossier build crée avec le manager d'extensions chrome

